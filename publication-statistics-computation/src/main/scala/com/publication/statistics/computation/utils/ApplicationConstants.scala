package main.scala.com.publication.statistics.computation.utils

/**
  * This class denotes a singleton class which stores all application level constants used across
  * the project.
  * */
object ApplicationConstants {

  val BUCKETING = "bucketing"
  val BUCKET_SIZE = "bucket-size"
  val MEAN_MEDIAN_MAX = "mean-median-max"
  val AUTHORSHIP_SCORE = "authorship-score"
  val JOB_TYPE = "job-type"
  val NODES_TO_ANALYZE = "nodes-to-analyze"
  val POSSIBLE_START_TAGS = "possible-start-tags"
  val POSSIBLE_END_TAGS = "possible-end-tags"
  val MAP_REDUCE_JOB_PIPELINE = "map-reduce-job-pipeline"
  val DBLP_PATH = "dblp-path"
  val JOB_NAME = "job-name"
  val JOBS = "jobs"
  val INPUT_PATH = "input-path"
  val OUTPUT_PATH_SUFFIX = "output-path-suffix"
  val SORT = "sort"
  val BUCKETING_BY_NUM_CO_AUTHOR = "bucketing-by-num-co-author"
  val BUCKETING_BY_PUBLICATION_TYPE = "bucketing-by-publication-type"
  val SORT_FIRST_JOB = "sort-first-job"
  val SORT_SECOND_JOB = "sort-second-job"
  val INTERMEDIATE_SORT_OUTPUT_PATH_SUFFIX = "intermediate-sort-output-path-suffix"
  val COMPLETE_SORT_OUTPUT_PATH_SUFFIX = "complete-sort-output-path-suffix"
}
